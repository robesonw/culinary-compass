{
  "name": "WearableSync",
  "type": "object",
  "properties": {
    "sync_date": {
      "type": "string",
      "format": "date",
      "description": "Date of the synced activity"
    },
    "source": {
      "type": "string",
      "enum": [
        "apple_health",
        "google_fit",
        "manual"
      ],
      "description": "Source of the data"
    },
    "steps": {
      "type": "number",
      "description": "Steps taken"
    },
    "active_calories": {
      "type": "number",
      "description": "Active/exercise calories burned"
    },
    "resting_calories": {
      "type": "number",
      "description": "Resting metabolic calories"
    },
    "sleep_hours": {
      "type": "number",
      "description": "Hours of sleep"
    },
    "sleep_quality": {
      "type": "string",
      "enum": [
        "poor",
        "fair",
        "good",
        "excellent"
      ],
      "description": "Sleep quality rating"
    },
    "resting_heart_rate": {
      "type": "number",
      "description": "Resting heart rate in bpm"
    },
    "body_weight": {
      "type": "number",
      "description": "Body weight in kg"
    },
    "body_fat_percentage": {
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "description": "Body fat percentage (0-100)"
    },
    "notes": {
      "type": "string",
      "description": "User notes about the day"
    },
    "synced_at": {
      "type": "string",
      "format": "date-time",
      "description": "When this data was synced"
    }
  },
  "required": [
    "sync_date",
    "source"
  ]
}